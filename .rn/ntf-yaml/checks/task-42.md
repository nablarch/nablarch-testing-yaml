# task-42 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 指示書 2-7 の表の4行（`:108`／`:136`、`:216`／`:433`／`:434`、`:209`／`:243`／`:275`、`:380`）が解説書の該当行と食い違わない | OK | 第1ラウンドで 9 箇所を書き換え、突き合わせを §A に全件記載した。第2ラウンド（§F-1）で `:108`・`:136`・`:216`・`:380`・`:433` をさらに明快さ・用語統一・欠落補充の観点で書き直し、`:11`・`:18`・`:293` を加えた。解説書との突き合わせは §F-1 の表と §F-4 に 1 件ずつ記載（S8・S9 は解説書に無いことを書くため「スキーマ自身が課す制約」「実装が課す制約」のどちらかを §F-4 で明示した）。行番号は 2 ラウンドとも不変（第2ラウンドの `git diff -U0` のハンクヘッダは `@@ -11 +11 @@`・`-18 +18`・`-108 +108`・`-136 +136`・`-216 +216`・`-293 +293`・`-380 +380`・`-433 +433` の 8 個だけ＝変更は 8 行、`additionalProperties`（`:429`-`:432`）と `$comment`（`:434`）は無変更）。解説書はピン `afa4f9e` を行番号つきに展開して照合した | | |
| 文言が解説書に沿っており、実装の挙動の写しになっていない | OK | 第1ラウンドの 4 群は解説書の文（`:1502`・`:1295`・`:1153`／`:1299`・`:889`／`:1155`）を主語にして書いた。第2ラウンドで `:216` から「キー名の検査は読み込み時に実装が行う」という実装寄りの一文を落とし、**利用者から見た挙動**（読み出したときに検査される／巻き添えにならない）に置き換え、実装のクラス名は `$comment`（`:434`、保守者向け）側だけに残して役割で二重化を解消した（§F-4）。解説書に無い 2 件（S8 の値の型・S9 の `
` 2 文字）は、どちらが課す制約かを本文に明記した。逆に、実装が解説書より厳しい／緩い箇所は `description` に書かず §C に挙げた（§C-4 は第2ラウンドで実測に基づき訂正済み — 解説書に出典が無いのは Boolean の NPE だけ） | | |
| 是正の前後で挙動テストの結果が変わらないことが示されている | OK | 第2ラウンドは `94f7e16`（第1ラウンドのコミット）と比較した。隔離 worktree（`git worktree add --detach … 94f7e16`。測定用の一時ファイルを削除した状態）と作業ツリーで同一コマンド `JAVA_HOME=/usr/lib/jvm/temurin-17-jdk-amd64 mvn -o clean test` を 1 回ずつ実行し、`^[INFO] Tests run:` の行から経過時間を落として `diff` → **無差分**（10 クラス＋合計）。合計はどちらも `Tests run: 318, Failures: 0, Errors: 0, Skipped: 0`。第1ラウンド（`cb82f3b` との比較）の実測は §D。詳細は §F-7 | | |
| `mvn -o clean test` が BUILD SUCCESS | OK | 第2ラウンド最終: `JAVA_HOME=/usr/lib/jvm/temurin-17-jdk-amd64 mvn -o clean test` → `[INFO] Tests run: 318, Failures: 0, Errors: 0, Skipped: 0` / `[INFO] BUILD SUCCESS` | | |

## §A 解説書との突き合わせ（1件ずつ・是正前後の差）

解説書の逐語はすべてピン `afa4f9e` の `testdata_notation.rst` を自分で開いて取得した。

### A. `:108`（`$defs.table_data.properties.rows`）・`:136`（`$defs.list_map_data.properties.rows`）— 2-4

- 解説書 `:1502`（節「コメント・マーカーカラム・空エントリを扱う」＝ `:1451` の見出し配下）逐語:
  「記法として空のエントリは読み飛ばされる。\ Excel\ 形式では行の全セルが空セルの場合、\ YAML\ 形式では ``rows:``\ 内の要素が空マッピング（\ ``{}``\ ）の場合である。\ ``""``\ と書いた空文字は値であり、すべての値が ``""``\ のエントリは読み飛ばされず、全カラムが空文字のエントリとして読み込まれる。（略）この判定はマーカーカラムを除外する前に行われる。そのため、マーカーカラムだけに値があるエントリは読み飛ばされず、他のカラムがすべて空文字のエントリとして読み込まれる。」
- **是正前の食い違い**: 見出しが「【全ての値が空文字の行は行として存在しない】」、本文が「空マッピング `{}` の行、**および全ての値が空文字 `""` の行**は、行が無いものとして取り除かれる」。解説書は全値 `""` の行を**読み飛ばさない**と明言しているので、真逆だった。またマーカーカラムについて「判定は行の全ての値を対象とし、マーカーカラムの値も含める」と実装寄りに書いており、解説書の「マーカーカラムを除外する**前に**行われる」という順序の説明が落ちていた。
- **是正後**: 見出しを「【記法として空のエントリだけが行として存在しない】」に改め、「値を1つも持たない空マッピング `{}` の行だけが、行が無いものとして取り除かれる。`""` と書いた空文字は値であり、すべての値が `""` の行は取り除かれず、全カラムが空文字の行として読み込まれる」とした。マーカーカラムは「この判定はマーカーカラム（…）を除外する前に行われるため、マーカーカラムだけに値がある行も取り除かれない」と、解説書の順序の説明に合わせた。
- 実装（参照のみ・変更なし）も同じ規則である: `src/main/java/nablarch/test/core/reader/yaml/YamlSection.java:212` の `dropBlankRows` が空マッピングと非 Map だけを落とす。是正は**スキーマ側の文言が実装と解説書の双方に遅れていた**ことの解消であって、挙動の変更ではない。

### B. `:216`（`message_data.fw_header` の `description`）・`:433`（`$defs.fw_header` の `description`）・`:434`（同 `$comment`）— 2-3

- 解説書 `:1295` 逐語（該当部）:
  「``fw_header:``\ に記載できるキーは、\ ``reader.fwHeaderfields``\ の名前（省略時は ``requestId``\ ・\ ``userId``\ ・\ ``resendFlag``\ ・\ ``resultCode``\ ）だけである。それ以外のキーがあるとエラーになる。」
- **是正前の食い違い**: `:216` は「記述したキーはすべて FW 制御ヘッダとして NTF に渡される（値でのフィルタリングは行われない）。キー名は…設定に**合わせること**（既定例: …）」。`:433` も「キー名は…設定に**合わせる**（既定例: …）。記述したキーはすべて FW 制御ヘッダとして NTF に渡される」。どちらも**任意キーが通る**と読め、解説書の「それ以外のキーがあるとエラーになる」と食い違っていた。`:434` の `$comment` も「additionalProperties で**任意のヘッダ名を許容する**」と、意図（設定依存なので静的スキーマで表現できない）ではなく結果だけを書いていた。
- **是正後**: `:216`・`:433` とも「記載できるキーは、プロジェクトの reader.fwHeaderfields 設定に指定した名前（省略時は requestId, userId, resendFlag, resultCode）だけであり、それ以外のキーがあるとエラーになる」に統一。`:216` にはさらに「許容されるキー名はこの設定に依存するためスキーマでは表現できず、キー名の検査は読み込み時に実装が行う」を添えた（指示 §6 が許可した範囲）。`:434` の `$comment` は「記載できるヘッダ名は reader.fwHeaderfields の設定に依存し静的スキーマでは表現できないため、directives(固定キー)とは異なりスキーマ側ではキーを列挙しない。additionalProperties は値の型(string)だけを検査し、キー名がその設定に含まれるかの検査は実装(YamlMessageBuilder)が電文の読み出し時に行う」とした。
- **`additionalProperties`（`:429`-`:432`）の構造は変更していない**（`git diff -U0` のハンクに `:429`-`:432` が現れないことで確認）。指示 §4 のとおり、締めると `YamlMessageBuilderTest/mixedFwHeaderKeysData.yaml` が成立しなくなるため。実際にキーを弾くのは `src/main/java/nablarch/test/core/reader/yaml/YamlMessageBuilder.java:328` の `throw new IllegalStateException("fw_header in " + source + " has unknown key '" + key + "'. …")`。

### C. `:209`（`message_data.records`）・`:243`（`expected_request_message_data.records`）・`:275`（`group_message_data.records`）— 2-2

- 解説書 `:1153` 逐語（該当部）:「ただし、電文のレコードレイアウトは1つであり、ファイルデータのように複数のレコードレイアウトを持たない。」／`:1299` 逐語:「``records:``\ に記述するレコードレイアウトは1つである。2つ以上記述するとエラーになる。」
- **是正前の食い違い**: 3 箇所とも、スキーマ本体は #37（`389fe6d`）で `minItems: 1` / `maxItems: 1`（`:207`-`:208`・`:241`-`:242`・`:273`-`:274`）を持っていたのに、`description` にはその制約が一言も書かれていなかった。エディタの補完で最初に読む説明が制約を伝えないのは、指示書 2-2 が挙げた不足そのものである（誤りではなく欠落）。
- **是正後**: 3 箇所とも先頭に「records に記述するレコードレイアウトは1つであり、2つ以上記述するとエラーになる（電文はファイルデータのように複数のレコードレイアウトを持たない）。」を追加し、既存の説明はそのまま残した。
- `:182`（`file_data.records`）は指示どおり**対象外**（ファイルは複数レコードレイアウトを持てる。解説書 `:1153` が「ファイルデータのように」と対比している）。`git diff` にも現れない。

### D. `:380`（`$defs.record_fragment.properties.rows` の `description`）— 2-1

- 解説書 `:889` 逐語:「  - 末尾のフィールドに ``null``\ と記述した場合は、形式によらず\ ``""``\ になる。後ろに値のあるフィールドがあれば\ null\ のまま保持される」／`:1155` 逐語（該当部）:「（末尾に ``null``\ と記述した場合の扱いも、ファイルデータと同じである）」
- **是正前の食い違い**: 「不足した末尾のフィールドは `""` として扱われる」だけで、**明示的に `null` と書いた末尾フィールド**の扱いが無かった。読み手は `null` と書けば Java null が残ると解釈しかねない（テーブル系 `:108` では `null` が Java null になると書いてあるため、なおさら紛らわしい）。
- **是正後**: 「末尾のフィールドに `null` と書いた場合も `""` になる（後ろに値のあるフィールドがあれば `null` のまま保持される）。」を追加。解説書 2 文をそのまま1文に畳んだ。
- 実装（参照のみ・変更なし）も同じ: `src/main/java/nablarch/test/core/reader/yaml/YamlFileBuilder.java:262` の `NablarchTestUtils.trimTailCopy(rowValues)`（#36 で導入）。

## §B D2・D3

### D2: `+2` ずれた出典の訂正（自分で数え直した）

指示 §3 の 13 箇所を、`grep -rEn 'testdata_notation\.rst' src/` と Python の機械抽出（`([a-z_/]+\.rst)\}?:\s*(\d+)` ＋同一行の続き番号）で数え直し、**13 箇所ちょうど**であることを確認した。`:1299` 以上を指す出典は他に無い。

| 訂正箇所（file:line） | 前 | 後 | 後の行の逐語（ピン `afa4f9e`） |
|---|---|---|---|
| `src/test/java/nablarch/test/core/db/YamlDateNotationTest.java:30`・`:113`・`:133` | `:1326`-`:1331` | `:1328`-`:1333` | `:1328`「日付は以下の形式で記述できる。」／`:1333`「時刻のミリ秒または全部を後置0埋めの形で省略できる。…例えば ``20210123123456``\ は「2021年1月23日 12時34分56秒000」と評価される。」 |
| `src/test/java/nablarch/test/core/db/YamlDateNotationTest/date.yaml:1` | `:1326-:1331` | `:1328-:1333` | 同上 |
| `src/test/java/nablarch/test/core/reader/YamlTestDataParserTest.java:356` | `:1337` | `:1339` | `:1339`「HTTP\ リクエストパラメータの値にアップロードファイルを指定したい場合は、値に ``${attach:ファイルパス}``\ と記述する。」 |
| `src/test/java/nablarch/test/core/reader/YamlTestDataParserTest/nativeTypes.yaml:33` | `:1337` | `:1339` | 同上 |
| `src/test/java/nablarch/test/core/reader/yaml/YamlTableDataBuilderTest.java:56`・`:914` | `:1313-:1320` | `:1315-:1322` | `:1315`「``${文字種,文字数}``\ で使用できる文字種は、以下の14種類に限定される。」／`:1322`「  * - 半角英字 / 半角数字 / … / 改行 / 外字」（list-table の 14 種類の行） |
| `src/test/java/nablarch/test/core/reader/yaml/YamlTableDataBuilderTest/nativeTypes.yaml:88` | `:1313-:1320` | `:1315-:1322` | 同上 |
| `src/test/java/nablarch/test/core/reader/yaml/YamlTableDataBuilderTest.java:967` | `:1322` | `:1324` | `:1324`「本記法は単独でも組み合わせても使用できる。例えば ``${半角英字,5}``\ は…」 |
| 同 `nativeTypes.yaml:133` | `:1322` | `:1324` | 同上 |
| `src/test/java/nablarch/test/core/reader/yaml/YamlTableDataBuilderTest.java:1000` | `:1441-:1443` | `:1443-:1445` | `:1443`「  * - 改行文字」／`:1445`「    - YAML\ のパーサが制御文字に変換する。…」 |
| 同 `nativeTypes.yaml:83` | `:1441-:1443` | `:1443-:1445` | 同上 |

**訂正後の行番号の確かめ方**: `git -C ../nablarch-document show afa4f9e:ja/…/testdata_notation.rst` をファイルへ落とし、`awk '{printf "%d\t%s\n", NR, $0}'` で行番号つきに展開して、1 件ずつ本文を目で読んだ。加えて、コミット直前に Python スクリプトで **`src/` 配下の全出典を機械的に開き直し**、各出典の指す行の本文を一覧出力して確認した（下記 §E-1）。**13 箇所まとめて**訂正したので、同一ファイル内で `:1322` が 2 つの意味を指す状態は生じていない。

### D4: `+2` ずれとは無関係な別種の出典の誤り 3 件（D2 の 13 箇所には含まれない）

**D2（`+2` ずれ 13 箇所）とは別件である。**指示が挙げた 13 箇所には含まれず、原因も違う（`nablarch-document@6ba3c83` の 2 行挿入とは無関係で、着手前から指す先が誤っていた）。#44 の報告ではスコープ拡張として D2 とは別々に挙げること。

§E-1 の全件検証（`:1299` 未満を指す出典も含む）で見つかった 3 件で、いずれも `src/test/java/nablarch/test/core/reader/YamlTrailingNullOracleTest.java` にある。指す先が**空行**だったり（`:887`）主張と無関係だったり（`:857`・`:886`）する。§8-1 の完了条件が「不一致 0 件」であるため、番号だけを実測値へ訂正した（形式の変更はしていない）。訂正後の 3 箇所は `YamlTrailingNullOracleTest.java:131`（`:856`）・`:237`（`:882`）・`:271`（`:882`）で、いずれも実物を開いて逐語が在ることを確認済み。

| file:line | 前 | 後 | 訂正の根拠 |
|---|---|---|---|
| `YamlTrailingNullOracleTest.java:131` | `:857` | `:856` | 主張は「可変長はフィールド長の行を持たない」。`:857` は code-block 内の「          → データ（1件以上）」で無関係。`:856` が「        → フィールド長（固定長のみ）」でこの主張そのもの |
| `YamlTrailingNullOracleTest.java:237` | `:887` | `:882` | javadoc が引用する「いずれか1つのフィールドに `""` と記述した行」が `:887` に無い（**`:887` は空行**）。`:882` に「…全フィールドを省略した行（\ Excel\ 形式ではいずれか1つのフィールドに ``""``\ と記述した行、\ YAML\ 形式では ``rows:``\ の値をすべて ``""``\ とした行）を書けば、全フィールドが\ ``""``\ のレコードとして保持される」と逐語で在る |
| `YamlTrailingNullOracleTest.java:271` | `:886` | `:882` | javadoc が引用する「末尾のフィールドの値を書かなければ、そのフィールドは `""` として扱われる」が `:886`（「  データ行の空セルの扱いには、次の2点の注意がある。」）に無い。`:882` の冒頭「フィールドの数だけ値を並べる必要はない。末尾のフィールドの値を書かなければ、そのフィールドは\ ``""``\ として扱われる。」が逐語 |

**コーディネータの作業指示（work-order）§4** に書かれた「`:1299` 未満を指す 37 箇所は触らないこと」は、D3 の**方式（節見出し方式）への書き換えを禁じる**趣旨と解した。この文言は `.rn/ntf-yaml/steering.md` には存在しない（`grep -n "触らない" .rn/ntf-yaml/steering.md` が 0 件、`grep -n "37" .rn/ntf-yaml/steering.md` の 3 件はいずれも別文脈）。前版でこれを「指示 §4」と書き steering.md の記述であるかのように読ませていたのは帰属の誤りであり、ここで訂正する。指す先が誤っている出典を正しい番号に直すことは形式の変更ではなく、§8-1 の「不一致 0 件」を満たすために必要と判断した。方式は行番号形式のまま変えていない。

### D3: 節見出し方式への切り替えの判断

**今回は 1 箇所も切り替えなかった。**

- 方式の実物を 2 つとも読んだ。`src/main/java/nablarch/test/core/reader/yaml/YamlMessageBuilder.java:56`（クラス javadoc）が「行番号は改版で腐るため、節見出しと引用文で示す」と明記し、`:60`-`:66` の 2 つの `<li>` で節見出し（「メッセージングのデータを記述する」節の「Excel形式の場合」項／「YAML形式の場合」項）＋逐語引用の対を 2 つ書いている。同 `:56`-`:57` には、同じ見出しが 8 回ずつ現れるため**親節を添えて一意化する**という工夫まで書かれている。`src/main/java/nablarch/test/core/reader/yaml/YamlSection.java:176`（`dropBlankRows` の javadoc）も同様で、「行番号は改版で腐るため引用文で示す」としたうえで `:177`-`:179` に `:1502` の 3 文を逐語引用している。
- **切り替えなかった理由（判断と根拠）**:
  1. 指示 §3 が「13 箇所すべてを節見出し方式へ書き換えるのは #42 の範囲を超える」と明示しており、まず `+2` の訂正を確実に行うことを優先した。今回は訂正対象が 13 箇所＋追加 3 件の計 16 箇所に増えたため、方式の書き換えを同時に行うと差分が「番号の訂正」と「文体の改変」の混在になり、レビューで**どちらが正しさに効いているか**が読み取れなくなる。
  2. （**前版の理由(2)(3) は取り下げる。**理由(2)「YAML フィクスチャの 1 行コメントに逐語引用を足すと膨らみ可読性を損なう」は**反例が同じ Step の成果物にある** — `src/test/java/nablarch/test/core/reader/yaml/YamlMessageBuilderTest/mixedFwHeaderKeysData.yaml:1`-`:13` が、節見出し＋逐語引用を 7 行のコメントで書き、行番号を 1 つも使っていない。理由(3)「節見出しだけでは節内のどの文か特定できず結局引用が要る」は**引用文を書く理由**であって、行番号方式を維持する理由になっていない。いずれも実物を開いて確認した。）
- **推奨（コーディネータ判断待ち）**: 方式の切り替えは**別タスクとして切り出す**のが妥当と考える。前版は対象を「`src/main` と javadoc に限る」としたが、**実測により線引きを改める**:
  - `src/test` にも節見出し方式の先例がある。`src/test/java/nablarch/test/core/reader/YamlBlankEntryOracleTest.java` は行番号出典が **0 件**（`grep -c 'rst:'` = 0）で、`:223`・`:240`・`:256`・`:273` の 4 箇所がすべて「節見出し＋逐語引用」である。
  - YAML フィクスチャにも先例がある（上記 `mixedFwHeaderKeysData.yaml:1`-`:13`）。**「フィクスチャは行番号形式のまま残す」という前版の線引きは実物に反しており、取り下げる。**
  - したがって推奨は「`src/main`／`src/test`／フィクスチャの別で切らず、**解説書の文を根拠として引く出典はすべて節見出し＋逐語引用へ寄せる**」である。残る行番号出典は `src/main` では `YamlFileBuilder.java:261` の 1 箇所だけなので、`src/main` から着手すれば差分が小さい。
  - ただし切り替えの是非と範囲はユーザー／コーディネータの判断事項であり、#42 の範囲で独断すべきでないと判断した。

## §C 実装と解説書の食い違い（`description` には書かず報告）

指示 §6 に従い、`description` へは書かずここに挙げる。**いずれも今回は変更していない。**

1. **`fw_header` の未知キー検査は「遅延実行」なので、解説書より緩い。** 解説書 `:1295` は「それ以外のキーがあるとエラーになる」と無条件に述べる。実装 `YamlMessageBuilder.java:328` の検査は `convertFwHeader` の中にあり、**実際に読み出す電文のエントリに対してだけ**走る（同 `:38` のクラス javadoc が「<b>実際に読み出すメッセージに対してのみ遅延実行</b>する」と明記）。したがって、同一ファイル内の**一度も読み出されないエントリ**に未知キーを書いてもエラーにならない。これは #38 が意図して選んだ設計（誤記エントリが他エントリを巻き添えにしない）であり、指示 §4 が構造を締めるなと言う理由でもある。`description` には「キー名の検査は読み込み時に実装が行う」とだけ書き、遅延の範囲までは書いていない（解説書に無い挙動のため）。
2. **`fw_header` の値は文字列でなければならない、という制約が解説書に無い。** スキーマ `$defs.fw_header` の `additionalProperties: {"type": "string"}`（`:430`-`:432`）は、クォートなしの数値・真偽値をロード時に弾く（`src/test/java/nablarch/test/core/reader/yaml/YamlMessageBuilderTest.java:1612`-`:1617` がこの前提を明記）。解説書 `:1295` は値の型に触れていない。**実装＋スキーマが解説書より厳しい**。既存の制約であり今回は触っていない。
3. **`record_fragment.rows` の「空配列 `[]` を1要素書くと全フィールドが `""` のレコード1件になる」は解説書に無い書き方。** 解説書 `:882` は「全フィールドを省略した行（…\ YAML\ 形式では ``rows:``\ の値をすべて ``""``\ とした行）」と、**すべて `""` と書く**やり方だけを示す。`[]` は解説書が定める「不足した末尾のフィールドは `""`」の規則から導ける結果ではあるが、解説書が例示した書き方ではない。**実装が解説書より緩い**（`[]` も受け付ける）。Step D の対象外なので既存文言を残した。
4. **`:108` のテーブル系の記述で解説書に出典を持たないのは Boolean 型の `NullPointerException` だけである（前版の記述を訂正）。** 前版は「日付型カラムに `""` は null 扱い」ほかカラム省略・FK・Boolean を一括して「解説書に出典を持たない実装由来の記述」としていたが、**実測すると Boolean の NPE 以外はすべて解説書に在る**。ピン `afa4f9e` の該当行を 1 件ずつ開いて確認した:
   - 「日付型カラムに `""` は null 扱い」→ `testdata_notation.rst:832`-`:833`（YAML 形式の表「日付型カラムの空文字／``""``\ （null 扱い）」）。Excel 形式の同じ表が `:770`-`:771`（「セルを空にする（null 扱い）」）
   - FK 制約違反 → `:666`「FK が設定された数値カラムも同様に省略できない。省略すると値 ``0`` が INSERT され、参照先テーブルに ID=0 の行がない場合は FK 違反でエラーになる。NULL にしたいカラムは、省略ではなく ``null`` を明示的に指定する」
   - カラム省略の2種類 → `:818`「カラム名は、最初の行（``rows:`` の先頭要素）のキーで決まる。後続の行がこのキーの一部を持たない場合、そのカラムは ``null`` を明示的に指定したのと同じ扱いになる。後続の行に最初の行のキーにないものを追加しても、そのキーは読み込まれない。」
   - デフォルト値補完 → `:693`（省略可能であること）・`:699`（`EXPECTED_TABLE` は比較対象外、`EXPECTED_COMPLETE_TABLE` はデフォルト値で比較）・`:701`-`:720`（型ごとのデフォルト値の list-table。数値型 ``0``／固定長文字は半角スペース×カラム長／可変長文字 ``" "``／日付型は epoch 起点／バイナリ型は 10 バイトのゼロバイト列の HexString／Boolean 型 ``"false"``）
   - **解説書に無いのは Boolean 型カラムの `NullPointerException` だけ**。ピン `afa4f9e` の `ja/development_tools/testing_framework/` 配下で `git grep -n "NullPointer"` が **0 件**（リポジトリ全体でも 1 件だけで、それは `.rn/` 配下の作業メモであり解説書本文ではない）。
   これは #42 の是正対象（指示書 2-7 の表の4行）に含まれないため今回も触っていない。Boolean の NPE は解説書側の記載漏れの候補として #43 で扱うのが妥当と考える。

## §D カバレッジ（JaCoCo・実測）

`task-41.md` と同じ手順（`rm -f jacoco.exec` → `mvn -o clean test` → `mvn -o org.jacoco:jacoco-maven-plugin:0.8.8:restore-instrumented-classes org.jacoco:jacoco-maven-plugin:0.8.8:report -Djacoco.dataFile=jacoco.exec` → `target/site/jacoco/jacoco.xml` を機械集計）で、**着手前（`git stash` で `cb82f3b` に戻した状態）と着手後の 2 回**計測した。

| | 行 | 分岐 | 命令 |
|---|---|---|---|
| 着手前（`cb82f3b`） | 413/417 | 174/176 | 1809/1822 |
| 着手後 | 413/417 | 174/176 | 1809/1822 |
| 差分 | **±0** | **±0** | **±0** |

クラス単位でも全 9 クラスが同値（2 回分の集計出力を `diff` にかけて無差分）。内訳は `YamlTestDataParser` 行 56/56・分岐 2/2、`InterpreterResolver` 2/2、`MessageContent` 6/6、`YamlFileBuilder` 85/86・41/42、`YamlLoader` 48/51・13/14、`YamlMessageBuilder` 87/87・38/38、`YamlSchemaValidationException` 7/7、`YamlSection` 53/53・50/50、`YamlTableDataBuilder` 69/69・30/30。

**変わらなかった理由**: 今回の変更は `src/main/resources` の JSON スキーマの `description`／`$comment` 文字列 9 行と、`src/test` の javadoc・YAML コメント 16 箇所の行番号だけである。`src/main/java` は 1 行も変更していない（`git diff --stat -- src/main/java` が空）。`description` はスキーマ検証の合否に影響しない注釈キーワードであり、実行される分岐を増減させない。

## §E コミット前の確認（指示 §8 の 1〜5）

1. **`src/` 配下の解説書出典を全件、機械的に検証した。不一致 0 件。**
   - 手順: ピン `afa4f9e` から `testdata_notation.rst`・`deal_unit_test/batch.rst`・`deal_unit_test/mom.rst`・`setup/common.rst`・`tools/master_data_tool.rst`・`class_unit_test/component.rst` の 6 ファイルを `git -C ../nablarch-document show afa4f9e:<path>` で取り出し、`src/` 全体から `([a-z_/]+\.rst)\}?:\s*(\d+)` と同一行の続き番号（`-:NNN` / `-{@code :NNN}` / `、:NNN`）を Python で抽出。**各出典が指す行の本文を1件ずつ出力**して読んだ。
   - 件数: **出典の指し先（範囲の両端をそれぞれ 1 点と数える）で 85 点**。うち `testdata_notation.rst` が 79 点、他 5 ファイルが 6 点。行番号を含む出典サイト（file:line 単位）は 55 箇所。存在しない行・空行・主張と無関係な行は **0 件**（訂正前に見つかった 3 件は §B のとおりこのコミットで訂正済み）。
   - 正規表現で拾えない**行またぎの出典**は目視で補った。`YamlTrailingNullOracleTest.java:31`-`:32`（`:889`（ファイルデータ）／次行の「同 `:1155`」（電文））の 2 点を個別に開いて確認（`:889` は末尾 `null` の規則、`:1155` は電文でも同じ扱い）。
   - 解説書以外の `file:line` 出典も同じ手順で開いた: `../nablarch-testing/.../MessageParser.java:107`-`:110`（`fwHeaderFields` の既定値 `requestId`・`userId`・`resendFlag`・`resultCode`。`YamlMessageBuilderTest.java:66`・`:124`・`:1078` が参照）、`ntf-testdata-yaml-schema.json:18`・`:25`・`:108`（`YamlColumnOmissionTest.java:34`・`:310` が参照。今回 `:108` の**カラム省略に関する記述は変更しておらず**、行番号も動いていない）、`fwHeaderMapData.yaml:41`-`:43`（`YamlMessageBuilderTest.java:1612` が参照する `"0"`・`"1234"`・`"true"` の 3 行）。
   - **1 件だけ、誤りではないが厳密でない記述を見つけたので報告する（未修正）**: `src/test/java/nablarch/test/core/reader/YamlTestDataParserTest.java:1859` の「`{@code :393}` が `{@code getMessageWithoutCache}` を呼ぶ」。実物の `../nablarch-testing/src/main/java/nablarch/test/core/messaging/SendSyncSupport.java:393` は `MessagePool pool = getMessages(basePath, resourceName, dataType, requestId);` で、`getMessageWithoutCache` を呼ぶのは同ファイル `:478`（`getMessages` の中）である。**間接的には正しい**が 1 段省いている。解説書出典ではなく本体ソース出典であり、#42 の対象（`description` と解説書出典）から外れるため今回は触らなかった。
2. **今回書いたスキーマ `description` の文言と解説書の該当文の突き合わせ**: §A に 4 群 9 箇所すべて記載。各群で「解説書の逐語」「是正前の食い違い」「是正後の文言」を対にした。
3. **今回書いた事実主張と、確かめた手段**
   - スキーマの `:108`・`:136`・`:209`・`:216`・`:243`・`:275`・`:380`・`:433`・`:434` の内容と行番号 → 編集前後に `sed -n '<n>p'` で 9 行を実際に表示。編集は Python で「対象文字列がファイル中にちょうど 1 個あること」を確認してから置換し、置換後に `json.load` で JSON として妥当なことを確認した。
   - `additionalProperties`（`:429`-`:432`）が無変更であること → `git diff -U0 -- <schema>` のハンクヘッダが 8 個（`:108`・`:136`・`:209`・`:216`・`:243`・`:275`・`:380`・`:433,2`）だけであることと、`sed -n '427,435p'` の表示で確認。
   - 解説書の逐語（`:856`・`:882`・`:889`・`:1153`・`:1155`・`:1295`・`:1299`・`:1315`・`:1322`・`:1324`・`:1328`・`:1333`・`:1339`・`:1443`・`:1445`・`:1451`・`:1502`） → ピン `afa4f9e` を行番号つきに展開して表示。
   - 実装が解説書と同じ規則であること → `YamlSection.java:212`（`dropBlankRows`）・`YamlFileBuilder.java:262`（`trimTailCopy`）・`YamlMessageBuilder.java:312`／`:328`（2 つの `throw`）をソースで確認。
   - D2 の件数が 13 であること → `grep -rEn 'testdata_notation\.rst' src/` と Python 抽出の 2 通りで数え直し。
   - 挙動が変わらないこと・カバレッジが変わらないこと → `mvn` を着手前後に実際に 2 回ずつ実行し、ログとカバレッジ XML を機械比較（§D および下記 4）。
4. **Step E（是正の前後で挙動テストの結果が変わらないことの実測）**: 上の完了条件表の 3 行目のとおり。`git stash` で `cb82f3b` に戻して 1 回、作業ツリーで 1 回、同一コマンドを実行。10 クラス × 4 指標が完全一致、合計も `Tests run: 318, Failures: 0, Errors: 0, Skipped: 0` で一致。
5. **`mvn -o clean test`**: `Tests run: 318, Failures: 0, Errors: 0, Skipped: 0` / `BUILD SUCCESS`。

## §F 第2ラウンド（レビュー4軸の指摘への対応。S1〜S14）

レビュー4軸はすべて pass、成果物に「実測で偽と断定できる記述」も 0 件だった。第2ラウンドは**文章としての明快さ・用語の統一・利用者向けチャネルの欠落**の是正であり、第1ラウンドの成果物に積み増した。**`src/main/java`・`src/test/java` のコードと `additionalProperties` などスキーマの構造は 1 文字も変更していない**（`git diff --stat -- src/main/java` は空。`git diff --stat -- src/test/java` は 2 ファイル 2 行で、内訳は `YamlTestDataParserTest.java:1855` の javadoc 1 行と `…/RM21AA0101/message.yaml:4` のコメント 1 行＝ともに S10 の出典番号の訂正のみ。スキーマは `git diff -U0` のハンクが `description` の 8 行だけ）。

### F-1 スキーマ `description`（`src/main/resources/nablarch/test/ntf-testdata-yaml-schema.json`）

行番号は書き換え後も不変（`git diff -U0` のハンクヘッダが `@@ -11 +11 @@`・`-18 +18`・`-108 +108`・`-136 +136`・`-216 +216`・`-293 +293`・`-380 +380`・`-433 +433` の 8 個だけ）。

| # | 箇所 | やったこと |
|---|---|---|
| S1 | `:108` | 末尾の括弧書きを、**空振りしていた対比**から**実際に残っている違い**へ差し替えた。旧: 「record_fragment の rows には適用されない（全フィールドが空文字のレコードは1件として保持される）」→ 新: 「除去の対象はテーブル系（table_data / list_map_data）の rows だけである。record_fragment の rows の要素は配列であり、値を1つも持たない空配列 `[]` を書いても取り除かれず、全フィールドが `""` のレコード1件として読み込まれる（テーブル系の空マッピング `{}` が取り除かれるのとは異なる）。」 |
| S2・S5 | `:108`・`:136` | 見出しを **結論が出る形**に、かつ**「エントリ」を使わない**形にした。旧: 【記法として空のエントリだけが行として存在しない】→ 新: 【`{}` の行だけが取り除かれ、すべての値が `""` の行は残る】 |
| S3 | `:216` | 「キー名の検査は**読み込み時**に実装が行う」を削除し、**利用者にとっての意味**に置き換えた: 「キー名の検査は、そのエントリの電文を読み出したときに行われる。誤記のあるエントリを読み出したときだけエラーになり、同一ファイル内の他のエントリの読み出しは巻き添えにならない。」 実装のクラス名は書かない（`$comment` との役割分担で二重化を解消。下記 F-4 参照） |
| S3 | `:433` | 同じ趣旨の一文を追加し、`:216` との非対称を解消した: 「このキー名の検査は、そのエントリの電文を読み出したときに行われる。」 |
| S4 | `:380` | 解説書 `:889` の文言（「後ろに値のあるフィールドがあれば `null` のまま保持される」）は**そのまま残し**、補足を追加した: 「ここでいう「値のあるフィールド」に `""` は含まれない。末尾側に `""` と `null` がどの順で並んでもまとめて `""` になり、`null` は保持されない（例: fields 3 件に対し `["x", null, ""]` と書くと `"x"`・`""`・`""` になる）。」 |
| S6 | `:11`・`:18`・`:108` | 「空行の除去」「空行除去後」という**見出しから消えた呼び名**を、S2 で決めた見出しの語に揃えて「`{}` の行の除去」に統一した。解説書 `:1502` が「この空エントリの省略は、可変長ファイルの空行を `""` で表現する記法とは別の仕組み」と区別しており、スキーマにも `:341`「[可変長専用] 空行を無視するか否か」が別に存在するため、「空行」の語をこの文脈から外した（`:341` は変更していない） |
| S7 | `:136` | `:108` からの逐語コピーだった一文を `list_map` の語彙に直し、この description の主題（テストコードへ渡すマップの件数）に答える形にした。旧: 「全カラムが空文字の行として読み込まれる」→ 新: 「全キーの値が空文字のマップ1件としてテストコードへ渡される」 |
| S8 | `:216`・`:433` | 兄弟ノード（`:108`・`:136`・`:389`）が持つ「数値・真偽値も必ず文字列（クォート付き）」を、**最も厳しい `fw_header` だけが持っていなかった**ので両方に足した: 「値は数値・真偽値も必ず文字列（クォート付き）で記述すること（例: requestId: `"0000000001"`）。値の型はキー名と違って**このスキーマ自身が課す制約**であり、クォートなしの数値・真偽値を1つでも書くとロード時にファイル全体がエラーになり、他のエントリも読み出せなくなる。」 `additionalProperties`（`:429`-`:432`）の構造は変更していない |
| S9 | `:293` | 無条件だった「シンボル以外の文字列を書いた場合は、その文字列自身が区切り文字になる」に、#40 以降の実態を書き足した: 「ただしバックスラッシュと `r` の2文字（`"\\r"`）を含む値はエラーになる（Excel 形式ではこの2文字が必ず CR に変換されるため、この2文字を含む値はテスティングフレームワークの仕様上存在しない）。」 兄弟の `field-separator`（`:333`）が同じ形で条件を書いており整合する |

### F-2 出典コメント（S10）

`notation.rst:1149` を「リクエスト ID と同じ名前のディレクトリ配下の固定名 `message.yaml` が読み込み単位」の根拠に挙げていた 2 箇所を `:1151` に直した。

| 訂正箇所 | 前 | 後 |
|---|---|---|
| `src/test/java/nablarch/test/core/reader/YamlTestDataParserTest.java:1855` | `:1149` | `:1151` |
| `src/test/java/nablarch/test/core/reader/YamlTestDataParserTest/sendSyncTestData/RM21AA0101/message.yaml:4` | `:1149` | `:1151` |

- 両方の行を自分で開いて確かめた。`:1149` は節「メッセージングのデータを記述する」の**導入段落**で `message.yaml` に触れていない（`setUpMessages`・`expectedMessages` が予約語であることを述べる段落）。`:1151` に「YAML\ 形式ではリクエスト\ ID\ と同じ名前のディレクトリ配下の ``message.yaml``\ である（\ ``message``\ は固定の名前）」が逐語で在る。
- 両箇所とも `deal_unit_test/mom.rst:72` を併記しており、そこに主張が逐語で在るため（`git show afa4f9e:…/deal_unit_test/mom.rst` の `:72`「YAML\ 形式ではリクエスト\ ID\ と同じ名前のディレクトリ配下の\ ``message.yaml``\ が読み込み単位になる（\ ``message``\ は固定の名前）」）、**前版も偽ではなかった**（節レベル指しの緩さ）。
- `:1149` を挙げる他 2 箇所（`YamlTestDataParserTest.java:1813`、`YamlTestDataParserTest/messageData.yaml:43`）は**予約 ID `setUpMessages`／`expectedMessages` の主張**であり `:1149` が正しい。自分で開いて確認したうえで**触っていない**。

### F-3 S4・S8・S9 の実測

隔離コピー（`git worktree add --detach … 94f7e16`）に一時テストクラス `S42MeasureTest` とフィクスチャを置いて実行し、標準出力に観測値を出した。**測定後、一時ファイルは削除し worktree も削除した。**

- **S4**（`["x", null, ""]` で `null` は保持されない）: 可変長ファイル `fields` 3 件に `rows: [["x", null, ""]]` を書いて `YamlTestDataParser#getSetupFile` → `DataFile#toDataRecords()`。結果は **`FIELD1="x"`・`FIELD2=""`・`FIELD3=""`（3 件とも String、null は無し）**、レコード 1 件。`[["x", null, "", null]]` でも同じ。理由は `NablarchTestUtils.trimTail`（`../nablarch-testing/src/main/java/nablarch/test/NablarchTestUtils.java:251`-`:263`）が `StringUtil.hasValue` の false な要素（null と `""` の両方）を末尾から削るため。呼び出しは `src/main/java/nablarch/test/core/reader/yaml/YamlFileBuilder.java:262` の `NablarchTestUtils.trimTailCopy(rowValues)`。
- **S8**（`fw_header` の値の型はロード時にファイル全体を落とす）: `messages` に正常エントリ `OK001` と、`fw_header.requestId: 12345`（クォートなし）の `BAD001` を同居させたファイルを用意。`YamlLoader.load` が **`YamlSchemaValidationException: … $.messages[1].fw_header.requestId: integer が見つかりました、string が予期されました`** で落ち、**`getMessage(…, "OK001")` も同じ例外で落ちた**（正常エントリすら読めない）。キー検査の遅延（S3）と違って巻き添えになることを実測で確認。`src/main/java/nablarch/test/core/reader/yaml/YamlMessageBuilder.java:41`-`:43` のクラス javadoc の記述と一致する。
- **S3 の裏取り**（キー検査は遅延する）: `YamlMessageBuilder.java:36`-`:38`「<b>実際に読み出すメッセージに対してのみ遅延実行</b>する」を実物で確認。既存テスト `src/test/java/nablarch/test/core/reader/yaml/YamlMessageBuilderTest.java:1218`（`YamlLoader.load` 成功）→ `:1221`（`goodKey001` は成功）→ 続く `try` で `badKey001` が `IllegalStateException` になることを固定している。
- **S9**（`record-separator` にバックスラッシュ＋`r` を書くとエラー）: 経路を実物で辿った — `YamlFileBuilder.applyDirectives`（`YamlFileBuilder.java:283`-`:292`。`:290` が `file.setDirective(e.getKey(), interpret(e.getValue(), interps, source))`）→ `YamlSection.interpret`（`YamlSection.java:296`。`:300` が `rejectLiteralCr(value, source)`）→ `YamlSection.rejectLiteralCr`（`:430`-`:441`）。実測では `record-separator: "\\r"` が **`IllegalStateException: A value containing a backslash followed by 'r' cannot be written in YAML format. … value=[\r], source=setup_files entry path='dummy/r1.csv'`**。対照として `record-separator: "|"` は正常に読み込めた（＝「シンボル以外の文字列自身が区切り文字になる」という既存文言はこの 2 文字を除いて成り立つ）。

### F-4 「スキーマ自身が課す制約」か「実装が課す制約」かの明示（指示 §「コミット前」2）

S8・S9 は解説書に無いことを書くため、どちらが課している制約かを本文で明示した。

| 追記した事実 | 誰が課すか | 本文での書き方 | 確かめ方 |
|---|---|---|---|
| `fw_header` の値は文字列（クォート付き） | **このスキーマ自身**（`$defs.fw_header` の `additionalProperties: {"type":"string"}`、`:429`-`:432`） | 「値の型はキー名と違ってこのスキーマ自身が課す制約であり」と明記 | F-3 の S8 実測（`YamlSchemaValidationException`＝スキーマ検証の例外） |
| `fw_header` のキー名の検査が読み出し時である | **実装**（スキーマは表現できない） | `description` には「そのエントリの電文を読み出したときに行われる」という**利用者から見た挙動**だけを書き、クラス名は `$comment`（`:434`）側に残した | F-3 の S3 裏取り |
| `record-separator` にバックスラッシュ＋`r` は書けない | **実装**（`YamlSection.rejectLiteralCr`）。解説書 `:1445` が値一般の規則として定めている | 主体を書かず解説書 `:1445` と同じ理由（「Excel 形式ではこの2文字が必ず CR に変換されるため…仕様上存在しない」）を添えた | F-3 の S9 実測と経路辿り |

`$comment`（`:434`、保守者向け）と `description`（`:216`・`:433`、利用者向け）の**二重化は役割で解消した** — `description` は「いつエラーになるか・巻き添えの有無」という利用者の判断材料だけを書き、`$comment` は「なぜスキーマがキーを列挙しないか・どのクラスが検査するか」という設計判断を書く。`:434` は変更していない（`git diff` に現れない）。

### F-5 コミット前の全件機械検証（第2ラウンド。S10 の訂正後）

`src/` 配下の解説書出典を Python で機械抽出し、ピン `afa4f9e` から取り出した解説書 6 ファイル（`implementation/testdata_notation.rst`・`implementation/deal_unit_test/mom.rst`・`implementation/deal_unit_test/batch.rst`・`setup/common.rst`・`tools/master_data_tool.rst`・`implementation/class_unit_test/component.rst`）の該当行を 1 件ずつ開いて本文を出力した。

- 抽出パターン: `([A-Za-z0-9_/]+\.rst)\}?\s*:\s*(\d+)` と、直後に続く範囲・並記の番号（`-:NNN`／`、:NNN`／`・:NNN`／`{@code :NNN}`）。
- 結果: **同一行で拾えた出典の指し先 80 点／出典サイト（file:line 単位）57 箇所。存在しない行・空行は 0 件。**（第1ラウンドの §E-1 が「85 点」としているのは範囲の両端の数え方が違うためで、どちらも同じ集合を見ている。今回は自分の抽出結果だけを根拠にする。）
- 正規表現で拾えない**行またぎの出典**を `grep -rnE "^\s*[#*]?\s*(同\s*)?\{?@?c?o?d?e?\s*:[0-9]{3,4}" src/` で洗い出したところ 2 件。`YamlTrailingNullOracleTest.java:32` の「同 `{@code :1155}`」は `testdata_notation.rst:1155` を指し、本文が「メッセージボディのデータ行でも、ファイルデータと同じく…（末尾に ``null``\ と記述した場合の扱いも、ファイルデータと同じである）」で主張どおり。もう 1 件の `YamlTestDataParserTest.java:1859` の `{@code :393}` は本体ソース（`SendSyncSupport.java`）の出典であり解説書出典ではない（下記 F-6 の 4 番目）。
- 行番号を持たない「節見出し＋逐語引用」形式の出典 20 箇所（`grep -rn "\.rst" src/` のうち同一行に数字を持たないもの）は、この機械検証の対象外である。方式そのものが行番号に依存しないため。

### F-6 記録のみ（#43／#44 でコーディネータが扱う。今回は着手していない）

1. **【別タスク候補】#40 の規則（バックスラッシュ＋`r` の 2 文字は書けない）がスキーマに一言も無い。** 解説書 `testdata_notation.rst:1445`・実装 `YamlSection.rejectLiteralCr`（`YamlSection.java:430`-`:441`）・テスト（`YamlSectionTest#interpret_emptyInterpretersStillRejectsLiteralCr` ほか）の三者は揃っているが、スキーマ側にこの規則を述べる記述が無い。今回 S9 で `record-separator`（`:293`）1 箇所だけを実態に合わせたが、この規則は**データ行・ディレクティブの値全般**に効く（`YamlFileBuilder.java:253` がデータ行の各セルを、`:290` がディレクティブ値を `interpret` に通す）。スキーマ全体への追記は #42 のスコープ外と判断した。
2. **【推奨・実施せず】行番号出典の構造的な腐り。** `nablarch-document` に行が挿入されるたびに `src/` 側の行番号出典がまとめてずれる（今回の D2 の 13 箇所がその実例）。対策として (a) F-5 のような**機械検証スクリプトを 1 本リポジトリに入れて CI か手順に組み込む**、(b) 出典を**ピン付きで書く**（`afa4f9e:…rst:889` の形）、(c) D3 の節見出し方式へ寄せる、が考えられる。(a) は今回スクラッチで書いたものをそのまま資産化できる。
3. **【起票候補・#43 向け】解説書 `testdata_notation.rst:889` の文言自体が曖昧である。** 逐語:「  - 末尾のフィールドに ``null``\ と記述した場合は、形式によらず\ ``""``\ になる。後ろに値のあるフィールドがあれば\ null\ のまま保持される」。ここでいう「値のあるフィールド」に `""` が含まれるかが読み取れない。**実測では含まれない** — `["x", null, ""]` は `"x"`・`""`・`""` になり `null` は保持されない（F-3 の S4）。実装 `NablarchTestUtils.trimTail`（`../nablarch-testing/src/main/java/nablarch/test/NablarchTestUtils.java:251`-`:263`）が `StringUtil.hasValue` の false な要素、すなわち null と `""` の**両方**を末尾から削るためである。`""` を「値」と呼ぶ用法は同じ解説書の `:1502`（「``""``\ と書いた空文字は値であり」）に在るため、`:889` の「値のあるフィールド」と読み手の中で衝突する。**`:889` に「（`""` は「値のあるフィールド」に含まれない）」相当の一言を足すことを提案する。**今回はスキーマ側（`:380`）に補足を書くにとどめ、解説書は変更していない（当リポジトリに権限が無い）。既存の申し送り `.rn/ntf-yaml/report-nablarch-document-discrepancies.md` と同じ性質の指摘だが、**そのファイルは今回書き換えていない**（#43 の扱い）。
4. 以下は着手前から存在し、#42 の対象外なので触っていない: `:108` 全体（約 2600 字）が `description` の器として過大である件（指示書 2-7 の対象外）／`YamlColumnOmissionTest.java:310` の鉤括弧が `:108` からの逐語引用でない件／`YamlTestDataParserTest.java:1859` の `SendSyncSupport.java:393` の 1 段省略（`:393` は `getMessages` を呼び、`getMessageWithoutCache` を呼ぶのは同ファイル `:478`。間接的には正しい。解説書出典ではない）。

### F-7 第2ラウンドの挙動テスト（`94f7e16` と比較）

隔離 worktree（`94f7e16` を detach でチェックアウトし、測定用の一時ファイルを削除した状態）と作業ツリーで、同一コマンド `JAVA_HOME=/usr/lib/jvm/temurin-17-jdk-amd64 mvn -o clean test` を 1 回ずつ実行し、`^\[INFO\] Tests run:` の行から経過時間を落として `diff` した。**無差分**（10 クラス＋合計）。合計はどちらも `Tests run: 318, Failures: 0, Errors: 0, Skipped: 0` / `BUILD SUCCESS`。

## QA / Expert Review

`94f7e16` について QA・Design・Craft(writing)・Verification(fact-check) の 4 軸を独立したサブエージェント
として実施した。**このタスクの媒体は「文章」**（利用者がエディタの補完で最初に読むスキーマ `description`）
なので、Craft は writing、Verification は fact-check として依頼した。

| 軸 | 判定 | 指摘 |
|---|---|---|
| QA | **pass** | 範囲外の観測 2 件・nit 2 件 |
| Design | **pass** | F1〜F6（うち F1・F2 が中） |
| Craft (writing) | **pass** | NG-1〜NG-7 |
| Verification (fact-check) | **pass** | 所見 3 件 |

**4 軸すべてが「成果物に実測で偽と断定できる記述は 0 件」と報告した。**#38（3 ラウンド）・#39（2 ラウンド）で
繰り返し落ちた「修正が新しい虚偽記述を混入させる」型は、#40・#41 に続き #42 でも起きていない。
残った指摘はすべて **「文としては真だが読み手を誤らせる／用語が割れている／利用者向けチャネルに欠けている」** 型である。

### 4 軸が独立に到達した実質的な発見

| 発見 | 検出した軸 | 内容 |
|---|---|---|
| **`:108` 末尾の括弧書きが、この是正が潰そうとした誤解を再生産していた** | Craft(NG-1)・Design(F4) | 旧モデル（テーブル系では全 `""` 行が消える）との**対比**として書かれていた括弧が、是正後は差を示さなくなり、読み手は逆に「テーブル系では全 `""` 行は保持されない」と推論する。実際の違いは「`record_fragment` の行は**配列**で `[]` でも消えない／テーブル系の**マッピング** `{}` は消える」 |
| **`fw_header` の値の型制約が利用者向けチャネルに無い** | Design(F1) | `additionalProperties: {"type":"string"}` はクォートなしの数値・真偽値をロード時に弾き、**キー検査と違って遅延せずファイル全体を落とす**。ところがこの事実は `$comment`（保守者向け）にしか無く、兄弟ノード `:108`・`:136`・`:389` は全部「数値・真偽値も必ず文字列で記述すること」を持つのに**最も厳しい `fw_header` だけが持たなかった** |
| **`:293` `record-separator` の `description` が #40 以降過大** | QA | 「シンボル以外の文字列を書いた場合は、その文字列自身が区切り文字になる」と無条件に書いていたが、#40 でディレクティブ値も `rejectLiteralCr` を通るため `record-separator: "\\r"` はエラーになる。**私たち自身の #40 が持ち込んだ不整合** |
| **`:216` の「読み込み時」が実測と食い違い、同じコミットの `$comment` とも不一致** | Craft(NG-3)・Verification(所見1) | 実測では `YamlLoader.load` は通り、そのエントリを**読み出したときにだけ**落ちる。利用者にとってこの差は大きい（1 エントリの誤記で他が巻き添えになるか否か） |
| **見出しの「エントリ」がスキーマ内の既存語彙と衝突** | Craft(NG-5) | スキーマ内の「エントリ」は既存 16 箇所すべてが**データブロック 1 件**を指す。新見出し 2 箇所だけが「エントリ＝データ行」の意味で、`setup_tables` の description を先に読んだ利用者は誤読する |
| **「空行」の呼び名が 3 箇所に取り残された** | Craft(NG-6)・Design(F3) | `:11` が参照する「rows で述べる空行の除去」という**呼び名が見出しから消えていた**。解説書 `:1502` は「可変長ファイルの空行とは別の仕組み」とわざわざ区別しており、混同を招く |
| **D3 の見送り理由 2 つが成り立たない** | Design | 「YAML フィクスチャに逐語引用を足すと膨らむ」の**反例が同じ Step の成果物**にある（`mixedFwHeaderKeysData.yaml:1`-`:13` が 7 行のコメントで節見出し＋逐語引用、行番号ゼロ）。Craft も「節見出し方式の先例は `src/test` にもある」（`YamlBlankEntryOracleTest.java` は行番号出典ゼロ）と独立に実測 |
| **チェックファイル §C-4 の主張が偽** | Verification(所見3) | 「解説書に出典を持たない実装由来の記述」としたものが実際は解説書に在った（日付型の `""` は `:832`-`:833`、FK は `:666`、カラム省略は `:818`、デフォルト値は `:701`-`:720`）。**解説書に無いのは Boolean の NPE だけ** |

### 軸ごとの評価

| 軸 | 観点 | 判定 | 根拠 |
|---|---|---|---|
| QA | 検証のやり方が目的に対して意味を持つか | OK | `description` を典拠に引用しているテストを全走査し、引用文字列がスキーマに逐語で残っているかを機械照合。`description`／`$comment` を除いた**構造 JSON が着手前と完全一致**することを Python で確認。#36〜#39 の 4 つの是正それぞれについてスキーマ 437 行・`description` 64 個を走査し直し漏れ 0 件を確認 |
| Design | システム全体の整合性（ドキュメント間の一貫性） | OK | 四者（解説書・スキーマ・実装・テスト）を突き合わせ、#36〜#39 の是正に取り残された箇所 0 件を確認。構造キーワードを行番号つきで抽出して着手前と `diff` し、構造が完全同一・行番号も同一であることを機械確認 |
| Design | 責務の分離 | OK（F1 のみ NG） | `$comment`（保守者向け）と `description`（利用者向け）の書き分けは保たれている。`:434` の書き換え（結果だけ → 設計判断の理由）は $comment の役目に合っている。F1 は「利用者向けの事実が保守者向けチャネルに落ちたまま」という線引きの誤り |
| Craft (writing) | 散文としての明快さと正しさ | OK（NG 4 件） | 二重否定なし・主語の欠落なし・`【】` の長さと体裁が既存と一致。「エラーになる」の語もスキーマ内多数派と一致。NG は見出しの読み取りにくさ・対比の空振り・時制語の食い違い・「値のあるフィールド」の曖昧さ |
| Craft (writing) | 既存の声・用語との一貫性 | OK（NG 3 件） | **良い判断を明記**: `:108` は解説書 `:1502` 末尾「マーカーカラムだけに値があるエントリは…他のカラムがすべて空文字のエントリとして読み込まれる」を**あえて落としている**。YAML でキーごと省略した他カラムは Java `null` になるため、そのまま写すと YAML について偽になる（`YamlBlankEntryOracleTest` T5 が本体との差として実測で固定） |
| Verification (fact-check) | 主張が一次情報と一致するか | OK | 出典 **85 点**をピン `afa4f9e` の 6 ファイルすべてに対して開き、存在しない行・空行・主張と無関係な行が 0 件であることを確認。差分 50 行がすべて javadoc・行コメント・`description`・`$comment` であること（コード変更ゼロ）を機械確認 |

### トリアージ

有効と判定して直したもの: **14 件（S1〜S14）**。内訳はスキーマ `description` 9 件・出典コメント 1 件・チェックファイル 4 件。
**`src/main/java`・`src/test/java` のコードは 1 行も変更していない**（`git diff --stat` はスキーマ JSON・テスト javadoc 1 行・フィクスチャコメント 1 行のみ）。

却下したもの（根拠つき。すべて #43／#44 へ記録）:

| 指摘 | 却下の根拠 |
|---|---|
| #40 の規則（`\` ＋ `r` の 2 文字）がスキーマ全体に一言も無い（Design F2） | 解説書 `:1445`・実装 `rejectLiteralCr`・テストの三者は揃っているがスキーマだけ無い。S9（`record-separator`）で実害のある 1 箇所は直したが、全体への追記は指示書 2-7 の対象（#36〜#39）外。**別タスク候補として記録** |
| 行番号出典の構造的な腐り（Design の推奨） | 57 サイトの出典に**リビジョンのピンが 1 つも無く**、壊れたことに気づく仕組みがゼロ。今回も #41 のレビューが偶然拾っただけ。Design は「機械検証スクリプトを 1 本入れる」「出典をピン付きで書く」を推奨。**#42 の範囲を超えるので推奨として記録** |
| 解説書 `:889` の「値のあるフィールド」が曖昧（Craft NG-4） | `NablarchTestUtils.trimTail` は `StringUtil.hasValue` が false の要素＝**null と `""` の両方**を末尾から削るため、`["x", null, ""]` では `null` が保持されない。#39 が「`""` は値」と確立したのと衝突する。**解説書側への起票候補として記録**（スキーマには曖昧さを潰す補足を S4 で追加済み） |
| `:108` 全体（約 2600 字）がスキーマ `description` の器として過大 | 指示書 2-7 の対象外 |
| `YamlColumnOmissionTest.java:310` の鉤括弧が `:108` からの逐語引用でない／`YamlTestDataParserTest.java:1859` の 1 段省略 | 着手前から存在し、今回の差分が触れていない |

### コーディネータの独立検証（`7daae89`）

- `JAVA_HOME=/usr/lib/jvm/temurin-17-jdk-amd64 mvn -o clean test` → `Tests run: 318, Failures: 0, Errors: 0, Skipped: 0` / `BUILD SUCCESS`（着手前と同数）
- **構造が変わっていないことを機械確認**: `git diff` の `+`/`-` 行のうち `"description":` と `"$comment":` 以外のキーを持つ行が **0 行**。`additionalProperties` は `$comment` の本文中に現れるだけで構造は無変更
- 解説書（ピン `afa4f9e`）の `:1502`・`:1295`・`:1153`・`:1299`・`:889` を開き、書き換えた `description` と突き合わせ
- **S4 の根拠を本体で確認**: `NablarchTestUtils.trimTail`（`../nablarch-testing/.../NablarchTestUtils.java:251`-`:263`）が `StringUtil.hasValue` の false な要素を末尾から削ることをソースで確認
- **S9 の根拠を確認**: `record-separator` の旧 `description` が無条件だったこと、#40 でディレクティブ値も検査経路を通ることを確認
- 見出しが【`{}` の行だけが取り除かれ、すべての値が `""` の行は残る】になり結論が前に出たこと、**スキーマ内の `description`／`$comment` の見出しに「エントリ」が 0 件**になったことを機械確認
- 「空行」の残存が `:341`（可変長の空行＝別概念）の **1 件のみ**であることを確認
- S10 の 2 箇所がともに `testdata_notation.rst:1151` に訂正されていることを確認

## Overall Verdict

- Self-check: **OK**（完了条件 4 件すべて OK。第2ラウンド〈§F〉でレビュー4軸の指摘 S1〜S14 に対応 — スキーマ `description` 8 行の書き直しと出典 2 箇所の訂正、および前版の誤りの訂正 3 件〈§C-4 の「解説書に出典が無い」、§B D4 の帰属「指示 §4」＝実際はコーディネータの work-order、§B D3 の理由(2)(3)＝反例あり〉。`src/main/java`・`src/test/java` のコードとスキーマの構造は無変更。判断が必要な論点は §B D3 の推奨〈節見出し方式へ寄せる範囲〉と §F-6 の記録 3 件〈#40 の規則がスキーマに無い／行番号出典の構造的な腐り／解説書 `:889` の文言の曖昧さ〉）
- Craft (writing): OK（pass。NG 7 件を S1〜S7 で是正。解説書 `:1502` 末尾をあえて落とした判断を「正しい」と評価）
- Design: OK（pass。F1 を S8 で是正。四者の整合で #36〜#39 の取り残し 0 件、構造の無変更を機械確認）
- Verification (fact-check): OK（pass。出典 85 点をピンに対して全件照合し不一致 0 件。所見 3 件を S3・S10・S11 で是正）
- QA: OK（pass。直し漏れ 0 件を全件走査で確認。`:293` の #40 由来の不整合を発見し S9 で是正）
- コーディネータの独立検証: OK（上記「コーディネータの独立検証」節）
- Ready to check off: Yes（完了条件 4 項目すべて OK。4 軸とも pass・虚偽記述 0 件、有効な指摘 14 件を 1 ラウンドで是正、却下 5 件は根拠を記録して #43／#44 へ送る。`mvn -o clean test` 緑をコーディネータが独立に実測）
